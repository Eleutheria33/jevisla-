{# empty Twig template #}
{# empty Twig template #}
{{ form_start(form, { 'action': path('jevisla_messagerie_conversation', {'id' : app.session.get('alerteId')}), 'attr': { 'class': 'jevisla_alerte_reponseAlerte' } }) }}
    <div class="form-group">
         {# Génération du label. #}
         {{ form_errors(form.message) }}
        <div class="col-sm-12">
            {# Génération de l'input. #}
            {{ form_widget(form.message, {'attr': {'class': 'form-control ckeditor'}}) }}
        </div>
    </div>
    <br>       
    {{ form_label(form.envoyer, "Envoyer", {'label_attr': {'class': 'control-label'}}) }}
    <div class="col-md-4 pull-left">
        {{ form_widget(form.envoyer, {'attr': {'class': 'btn btn-info'}}) }}              
    </div> 
{{ form_end(form) }}  